# ─────────────────────────────────────────────────────────────────────────────
# COMPSCI 426 — Team Project compose.yml
#
# This file defines the Holmes investigation container and a shared network.
# Add your own services below the "Add your services here" section.
#
# Every service you add must be attached to `team-net` so it is reachable
# from holmes by service name.
#
# Quick reference:
#   Start everything:          docker compose up --build
#   Open a shell in holmes:    docker compose exec holmes bash
#   View logs:                 docker compose logs -f
#   Check health:              docker compose ps
# ─────────────────────────────────────────────────────────────────────────────

networks:
  team-net:
    driver: bridge

services:

  # ── Holmes — System Investigation Container ────────────────────────────────
  # A persistent container with curl, wget, jq, k6, psql, redis-cli, Node,
  # Python, bat, lsd, fd, ripgrep, Neovim + LazyVim, and more.
  #
  # Use it to inspect and test your services from inside the Docker network:
  #
  #   docker compose exec holmes bash
  #   curl http://order-service:3000/health | jq .
  #   psql postgres://user:pass@postgres:5432/mydb
  #   redis-cli -h redis ping
  #   k6 run /workspace/k6/sprint-1.js
  #
  # Your repo root is mounted at /workspace, so all k6 scripts and files
  # are accessible without copying them into the container.
  #
  # See holmes/README.md for a full tool reference.
  holmes:
    build:
      context: ./holmes
      dockerfile: Dockerfile
    container_name: holmes
    restart: unless-stopped
    volumes:
      - .:/workspace
    networks:
      - team-net
    environment:
      - TERM=xterm-256color
    healthcheck:
      test: [ "CMD-SHELL", "echo 'healthy'" ]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
  # Holmes has no ports — access it via: docker compose exec holmes bash

  # ── Ingestion Service ──────────────────────────────────────────────────────
  ingestion:
    build:
      context: .
      dockerfile: ingestion/Dockerfile
    container_name: ingestion-service
    restart: unless-stopped
    depends_on:
      ingestion-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgres://user:pass@ingestion-db:5432/ingestion-db
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=ingestion-service
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:3001/health >/dev/null || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ── Ingestion Database ──────────────────────────────────────────────────────
  ingestion-db:
    image: postgres:16
    container_name: ingestion-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: ingestion-db
    volumes:
      - ingestion-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d ingestion-db" ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Dashboard API ──────────────────────────────────────────────────────
  dashboard-api:
    build:
      context: .
      dockerfile: dashboard-api/Dockerfile
    container_name: dashboard-api
    restart: unless-stopped
    depends_on:
      readings:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - DATABASE_URL=postgres://user:pass@readings:5432/readings-db
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=dashboard-api
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:3000/health >/dev/null || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ── Readings Database ──────────────────────────────────────────────────────
  readings:
    image: postgres:16
    container_name: readings
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: readings-db
    volumes:
      - readings-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d readings-db" ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Sensor Registry Database ──────────────────────────────────────────────────────
  sensor-registry:
    image: postgres:16
    container_name: sensor-registry
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: sensor-registry-db
    volumes:
      - sensor-registry-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d sensor-registry-db" ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Alert Database ──────────────────────────────────────────────────────
  alert:
    image: postgres:16
    container_name: alert
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: alert-db
    volumes:
      - alert-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d alert-db" ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Report Database ──────────────────────────────────────────────────────
  report:
    image: postgres:16
    container_name: report
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: report-db
    volumes:
      - report-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d report-db" ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Device Database ──────────────────────────────────────────────────────
  device:
    image: postgres:16
    container_name: device
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: device-db
    volumes:
      - device-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U user -d device-db" ]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7
    container_name: redis
    restart: unless-stopped
    networks:
      - team-net
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Add your services here ─────────────────────────────────────────────────
  #
  # Pattern for each service:
  #
  # your-service:
  #   build: ./your-service
  #   container_name: your-service
  #   restart: unless-stopped
  #   ports:
  #     - "3001:3001"          # expose on host for local testing
  #   networks:
  #     - team-net             # REQUIRED — makes it reachable from holmes
  #   environment:
  #     - DATABASE_URL=postgres://user:pass@postgres:5432/yourdb
  #     - REDIS_URL=redis://redis:6379
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 3
  #     start_period: 15s
  #
  # ── Postgres template ──────────────────────────────────────────────────────
  #
  # postgres:
  #   image: postgres:16
  #   container_name: postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: pass
  #     POSTGRES_DB: mydb
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   networks:
  #     - team-net
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U user -d mydb"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 5
  #
  # ── Redis template ─────────────────────────────────────────────────────────
  #
  # redis:
  #   image: redis:7
  #   container_name: redis
  #   restart: unless-stopped
  #   networks:
  #     - team-net
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 5

  # ── Sensor Registry Service ─────────────────────────────────────────────
  sensor-registry-service:
    build:
      context: .
      dockerfile: sensor-registry/Dockerfile
    container_name: sensor-registry-service
    restart: unless-stopped
    networks:
      - team-net
    environment:
      - DATABASE_URL=postgres://user:pass@sensor-registry:5432/sensor-registry-db
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=sensor-registry-service
    depends_on:
      sensor-registry:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:3000/health >/dev/null || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── Alert Service ───────────────────────────────────────────────────────
  alert-service:
    build:
      context: .
      dockerfile: alert-service/Dockerfile
    container_name: alert-service
    restart: unless-stopped
    networks:
      - team-net
    environment:
      - DATABASE_URL=postgres://user:pass@alert:5432/alert-db
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=alert-service
    depends_on:
      alert:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://localhost:3000/health >/dev/null || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── Device Management Service ───────────────────────────────────────────
  device-management-service:
    build:
      context: .
      dockerfile: device-management/Dockerfile
    container_name: device-management-service
    restart: unless-stopped
    networks:
      - team-net
    environment:
      - DATABASE_URL=postgres://user:pass@device:5432/device-db
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=device-management-service
    depends_on:
      device:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:3000/health >/dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
# ── Named volumes ──────────────────────────────────────────────────────────────
# Declare a volume for each database here, e.g.:
#
volumes:
  readings-data:
  sensor-registry-data:
  alert-data:
  report-data:
  device-data:
  ingestion-data:
